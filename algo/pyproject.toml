[project]
name = "mayue-algo"
version = "0.1.0"
description = "Mayue algorithm module"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
