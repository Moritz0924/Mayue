version: "3.9"
services:
  ollama:
    image: ollama/ollama:latest
    container_name: mayue-ollama
    ports:
      - "11434:11434"
    volumes:
      - E:\OllamaModels:/root/.ollama
    restart: unless-stopped
